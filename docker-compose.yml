version: '3.8'
services:
  myevents:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - myevents_data:/app/data
    environment:
      - JWT_SECRET=change_me_in_production
      - PORT=3000
    restart: unless-stopped
volumes:
  myevents_data:
